{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "github-clone",
  "projectType": "application",
  "root": "apps/github-clone",
  "sourceRoot": "apps/github-clone/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "command": "npm run build --workspace=apps/github-clone"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run lint --workspace=apps/github-clone"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run typecheck --workspace=apps/github-clone"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run dev --workspace=apps/github-clone"
      }
    }
  }
}

{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "substack-clone",
  "projectType": "application",
  "root": "apps/substack-clone",
  "sourceRoot": "apps/substack-clone/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "command": "npm run build --workspace=apps/substack-clone"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run lint --workspace=apps/substack-clone"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run typecheck --workspace=apps/substack-clone"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run dev --workspace=apps/substack-clone"
      }
    }
  }
}

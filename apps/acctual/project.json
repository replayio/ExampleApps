{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "acctual",
  "projectType": "application",
  "root": "apps/acctual",
  "sourceRoot": "apps/acctual/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "command": "npm run build --workspace=apps/acctual"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run lint --workspace=apps/acctual"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run typecheck --workspace=apps/acctual"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run dev --workspace=apps/acctual"
      }
    }
  }
}

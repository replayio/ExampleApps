{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "digg-clone",
  "projectType": "application",
  "root": "apps/digg-clone",
  "sourceRoot": "apps/digg-clone/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "command": "npm run build --workspace=apps/digg-clone"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run lint --workspace=apps/digg-clone"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run typecheck --workspace=apps/digg-clone"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run dev --workspace=apps/digg-clone"
      }
    }
  }
}
